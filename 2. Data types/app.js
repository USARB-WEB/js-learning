/* Number */
let a = 12;
let b = 3.14;
let name = "Ion";

// Divisions by zero in JS = Infinity
console.log(a / 0);
// Division between a number and not a number = NaN
console.log(a / name);
// Division between not a number and number = NaN
console.log(name / a);